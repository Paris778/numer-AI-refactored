# Campaign benchmark-rebuild-v1 — lgbm_v6: Numerai packaged medium set (780).
run:
  name: brb1-lgbm-v6
  seed: 20260810

data:
  version: v5.3
  feature_set: all
  feature_subset: medium

  targets:
    - target

split:
  scheme: walk_forward
  purge_eras: 8
  n_folds: 4

model:
  backend: lightgbm
  preset: fast

evaluation:
  backend: custom
  main_target: target
  metrics: [corr, mmc, fnc, sharpe]
  validation_scorecard: true
